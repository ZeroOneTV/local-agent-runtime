import { DEFAULT_TOOL_GUIDANCE } from './prompt-template.service';

/**
 * Mantido para o caminho não-nativo (llm.service/context.service).
 * Fonte de verdade única: DEFAULT_TOOL_GUIDANCE em prompt-template.service.ts.
 */
export const TOOL_USE_PROMPT = DEFAULT_TOOL_GUIDANCE;
