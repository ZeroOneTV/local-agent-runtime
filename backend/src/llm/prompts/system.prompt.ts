import { DEFAULT_PERSONA } from './prompt-template.service';

/**
 * Mantido para o caminho não-nativo (llm.service/context.service).
 * Fonte de verdade única: DEFAULT_PERSONA em prompt-template.service.ts.
 */
export const SYSTEM_PROMPT = DEFAULT_PERSONA;
